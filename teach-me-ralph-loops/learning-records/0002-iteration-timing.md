# Single Ralph iteration takes ~4 minutes for this task

First live run of once.sh completed Experiment 1 (happy path) in ~4 minutes (10:28 to 10:32). This is shorter than the initial 12-minute estimate from the earlier test run. The "fire it off, explain while it runs" presentation structure still works -- the run fills the first half of the talk nicely, and results are ready to show by minute 7-8.

**Evidence:** Timestamped once.sh output from presentation-test-run copy.

**Implications:** The presentation timing is comfortable. Even if a run takes longer (network latency, more complex experiment), the safety net of output-backup/ covers it. The results file is detailed enough to be a compelling "check back" moment in the talk.
