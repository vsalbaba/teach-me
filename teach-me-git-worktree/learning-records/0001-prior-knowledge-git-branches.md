# Prior knowledge: git branches and Claude Code

User is comfortable with git branches (create, switch, merge) and uses Claude Code CLI. No need to teach branch fundamentals. Can jump straight to worktree concepts and multi-agent workflow design.
